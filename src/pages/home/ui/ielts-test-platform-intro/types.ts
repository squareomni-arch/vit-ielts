export interface TestPlatformIntroConfig {
  badge: string;
  title: string;
  titleHighlight: string;
  cards: Array<{
    title: string;
    icon: string;
    bg: string;
    color: string;
    href: string;
  }>;
}
